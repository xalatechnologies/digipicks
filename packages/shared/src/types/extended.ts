/**
 * Extended types for backoffice/admin UI
 *
 * These extend SDK types with additional fields that the backoffice UI
 * displays but the SDK types don't yet define. As the SDK evolves,
 * fields should be moved from here to the canonical SDK types.
 */
